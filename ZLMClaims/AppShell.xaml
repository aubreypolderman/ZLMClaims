<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ZLMClaims.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ZLMClaims"
    xmlns:views="clr-namespace:ZLMClaims.Views"
    xmlns:lang="clr-namespace:ZLMClaims.Resources.Languages"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <ShellContent
            Title="{x:Static lang:AppResources.TabItemHome}"
            Icon="logo.svg"
            Route="MainPage"
            ContentTemplate="{DataTemplate local:MainPage}"/>

        <ShellContent 
            Title="{x:Static lang:AppResources.TabItemClaim}"
            Icon="claim.svg"
            Route="AllPhotosPage"
            ContentTemplate="{DataTemplate views:AllClaimsPage}"/>

        <Tab 
            Title="{x:Static lang:AppResources.TabItemRepair}"
            Icon="search.svg">

            <ShellContent
                Title="{x:Static lang:AppResources.TabItemRepairCooperative}"
                ContentTemplate="{DataTemplate views:AllRepairCompaniesPage}"/>

            <ShellContent
                Title="{x:Static lang:AppResources.TabItemRepairUncooperative}"
                ContentTemplate="{DataTemplate views:AllRepairCompaniesPage}"/>
        </Tab>

        <ShellContent 
            Title="{x:Static lang:AppResources.TabItemProfile}"
            Icon="profile.svg"
            ContentTemplate="{DataTemplate views:UserPage}"/>
    </TabBar>

</Shell>
